#include "main.h"

/**
* print_diagonal - check if charcter is an alphabet
* @n: the length of the line
* Return: 1 if alphabet and 0 otherwise
*/
void print_diagonal(int n)
{
	int i, x;

	for (i = 0; i < n; i++)
	{
		for (x = 0; x < j; x++)
			_putchar(' ');

		_putchar('\\');
		_putchar('\n');
	}

}
