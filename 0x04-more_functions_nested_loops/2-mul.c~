#include "main.h"

/**
* mul - check if charcter is an alphabet
* @a: the first
* @b : the second
* Return: 1 if alphabet and 0 otherwise
*/
int _isdigit(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
