#include "main.h"

/**
* print_numbers - check if charcter is an alphabet
* Return: 1 if alphabet and 0 otherwise
*/
int print_numbers(void)
{
	int i:

	for (i = 48; i <= 57; i++)
		_putchar(i);

		return (0);
}
