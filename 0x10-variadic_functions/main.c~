#include "variadic_functions.h"
#include <stddef.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_all("swwsifcsww", "hello", "bakka", 22, 33.5, 'A', NULL);
    print_all("swws", "hello", "bakka", 22, 33.5, 'A', NULL);
    return (0);
}