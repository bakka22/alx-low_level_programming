#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char str[] = "-abobaker55  mohamed22 and 5223";  
	int n;
	n = _atoi(str);
	printf("%d\n", n);
    return (0);
}