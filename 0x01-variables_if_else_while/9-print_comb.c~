#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */
/**
*main - the main function
* Return: 0 (success)
*/
int main(void)
{
	int n;

	for (n = 48 ; n <= 57 ; n++)
	{
		if (n != 57)
		{
			putchar(n);
			putchar(',');
			putchar(' ');
		}
		else
			putchar(n);
	}
	putchar('\n');

	return (0);
}
