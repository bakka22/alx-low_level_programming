/**
*op_add - add two numbers
*@a : the first number
*@b : the second number
*Return: the sum of the two numbers
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
*op_add - add two numbers
*@a : the first number
*@b : the second number
*Return: the sum of the two numbers
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
*op_add - add two numbers
*@a : the first number
*@b : the second number
*Return: the sum of the two numbers
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
*op_add - add two numbers
*@a : the first number
*@b : the second number
*Return: the sum of the two numbers
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
*op_add - add two numbers
*@a : the first number
*@b : the second number
*Return: the sum of the two numbers
*/
int op_add(int a, int b)
{
	return (a + b);
}
