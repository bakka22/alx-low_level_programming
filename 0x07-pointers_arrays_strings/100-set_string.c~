#include <stdio.h>
/**
* set_string - check if charcter is an alphabet
* @s: pointer argument
* @to : pointer argument
* Return: 1 if alphabet and 0 otherwise
*/
void set_string(char **s, char *to)
{
	**s = *to;
}
